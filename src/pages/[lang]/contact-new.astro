---
export const prerender = false;

import LayoutNew from '../../layouts/LayoutNew.astro';
import { useTranslations } from '../../i18n/utils';

// Import new redesigned components
import ContactSectionNew from '../../components/new/ContactSectionNew.astro';

const { lang } = Astro.params || { lang: 'en' };

if (lang && typeof lang === 'string' && !['en', 'es'].includes(lang)) {
	return new Response(null, {
		status: 404,
		statusText: 'Not Found',
	});
}

const t = useTranslations(lang as 'en' | 'es');
---

<LayoutNew
	title={t('contactNew.seo.title')}
	description={t('contactNew.seo.description')}
	lang={lang}
>
	<ContactSectionNew lang={lang} />
</LayoutNew>
