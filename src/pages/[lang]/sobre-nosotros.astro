---
// Redirect page for Spanish "sobre-nosotros" route
export async function getStaticPaths() {
	return [
		{ params: { lang: 'es' } },
	];
}

const { lang } = Astro.params;
return Astro.redirect(`/${lang}/about`, 301);
---

