---
// SSR mode - Redirect page for Spanish "privacidad" route
export function getStaticPaths() {
	return [{ params: { lang: 'es' } }];
}

const { lang } = Astro.params;
return Astro.redirect(`/${lang}/privacy`, 301);
---
